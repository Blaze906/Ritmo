{
  "name": "ritmo",
  "version": "0.1.0",
  "description": "Ritmo Android app for offline personal planning, reminders, tasks and calendar.",
  "private": true,
  "type": "module",
  "scripts": {
    "prepare:web": "node scripts/prepare-web.mjs",
    "android:add": "cap add android",
    "android:sync": "npm run prepare:web && cap sync android",
    "android:open": "cap open android"
  },
  "dependencies": {
    "@capacitor/android": "8.3.2",
    "@capacitor/core": "8.3.2"
  },
  "devDependencies": {
    "@capacitor/cli": "8.3.2"
  }
}
